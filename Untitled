```
DIAGNOSTIC + FIX: Founder WhatsApp notification gap

Before writing any code, read in this order:
1. /CLAUDE.md — 
2. /docs/whatsapp-page.md 
3. /src/lib/services/lead-ingestion.ts — full file
4. /src/lib/services/whatsapp-api.ts — full file
5. /src/lib/actions/leads.ts — full file (specifically: assignLead, createManualLead)
6. The migration that created whatsapp_notification_logs (search supabase/migrations/ for the table definition)

Do not write a single line of code until all six are read.

---

PHASE 1 — DIAGNOSE (output findings only, no code yet)

Answer all of the following by reading the actual source files:

1. Call sites of sendFounderLeadNotification
   - List every file + line where it is called.
   - Is it called inside ingestLead()? Inside assignLead()? Inside createManualLead()? Nowhere else?

2. Domain filtering
   - Does sendFounderLeadNotification have any domain conditional (if/switch/includes) that
     would skip certain domains? If yes, paste the exact condition.
   - Is there a domain allow-list anywhere in whatsapp-api.ts?

3. lead_id in logNotification
   - What is the signature of logNotification()?
   - What is passed as lead_id at every call site?
   - Why is lead_id null in every row of whatsapp_notification_logs?
   - Is lead_id nullable on the table? Check the migration.

4. assignLead code path
   - Does assignLead call sendFounderLeadNotification? If not, does it call anything in
     whatsapp-api.ts at all?
   - Does createManualLead call sendFounderLeadNotification?

5. Gupshup status 202
   - All 37 logged rows show gupshup_status=202 and delivered=true. Confirm: the problem
     is upstream (rows not created), not downstream (delivery failure).

---

PHASE 2 — FIX

After Phase 1 output is confirmed, apply these fixes:

Fix A — Wire founder alert into assignLead
If assignLead does not call sendFounderLeadNotification, add the call after the assignment
succeeds. Pattern must match the ingestLead call: fire-and-forget, wrapped in void, never
throws, never blocks the action return. Reference: invariant #5 in whatsapp-page.md.

Fix B — Wire founder alert into createManualLead (if also missing)
Same pattern as Fix A.

Fix C — lead_id logging bug
Identify why lead_id is not reaching logNotification. Fix the call site to pass the correct
lead_id. Do not change the logNotification signature if it already accepts lead_id. If the
table column is nullable and should not be, do NOT alter the migration (migrations never
change after run — CLAUDE.md rule). Add a non-nullable constraint only in a NEW migration
with the correct date prefix if warranted, and only after Fix A/B are in place so future
rows will always have a lead_id.

Fix D — Domain filter removal (if one exists)
If sendFounderLeadNotification has a domain allow-list, remove it. The founder receives
alerts for all domains. If a domain was recently added to the system and not added to the
list, that is the shop bug. Remove the list entirely — do not maintain a list.

---

PRE-MORTEM

Watch for:
- Adding a blocking await inside the fire-and-forget wrapper — this violates invariant #5
  and will cause assignLead to fail if Gupshup is down
- Calling createAdminClient() inside assignLead — whatsapp-api.ts already owns that; do not
  import admin client into the action layer
- Triggering a duplicate founder alert if both ingestLead AND assignLead now call the
  function for the same lead — add a guard: only fire from assignLead when it is a manual
  reassignment, not the initial webhook-triggered assignment (check whether assigned_to was
  previously null vs already set)

---

SIGN-OFF CONDITIONS

✓ pnpm tsc --noEmit passes zero errors
✓ Assign a lead manually from the dossier → founder_alert row appears in
  whatsapp_notification_logs with a non-null lead_id
✓ Create a lead manually via Add Lead modal with domain=shop → founder_alert row appears
✓ A lead arriving via the ingestion webhook → founder_alert still fires (ingestLead path
  not broken)
✗ lead_id is null in any new notification log row
✗ sendFounderLeadNotification throws or propagates an error to its caller

---

After sign-off:
1. Update src/lib/actions/CLAUDE.md with the confirmed call-site pattern for founder alerts
2. Add one line to docs/changelog.md: "Fix: founder WhatsApp alerts now fire on manual
   assignment and createManualLead paths; lead_id logging corrected — [date]"
```