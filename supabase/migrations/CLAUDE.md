# Migrations CLAUDE.md

## Rules

- Never edit a migration that has already run in production (rule A-14). Write a new one.
- Every new table gets `ALTER TABLE x ENABLE ROW LEVEL SECURITY` (rule A-08).
- Log and activity tables are append-only — no UPDATE or DELETE RLS policies, ever (rule A-11).
- Reuse `update_updated_at()` — never recreate it. It is defined in the earliest migration.
- All `SECURITY DEFINER` functions must have `SET search_path = public` (rule A-10).
- A `SECURITY DEFINER` read RPC must either **self-derive** its scope (`auth.uid()` /
  `get_user_role()` / `get_user_domain()` inside the body) — then it may be GRANTed to
  `authenticated` — or, if it accepts caller-supplied scope params (`p_role`/`p_domain`/
  `p_user_id`) or returns whatever slice it is asked for, its EXECUTE **must** be REVOKEd from
  `PUBLIC, anon, authenticated` in the same migration and it becomes admin-client-only from the
  service layer with session-derived args (Q-13; pattern: 0007/0008/0102 — audit F-1).

## Migration inventory

| File | What it does |
| --- | --- |
| `20260526000001_profiles.sql` | `user_role`, `app_domain` enums; `profiles` table; `update_updated_at()` function |
| `20260526000002_agent_routing_config.sql` | `agent_routing_config` table; round-robin helpers |
| `20260527000003_leads.sql` | `leads`, `lead_activities`, `lead_notes`, `tasks`, `task_gia_meta` |
| `20260527000004_lead_raw_payloads.sql` | `lead_raw_payloads` immutable log |
| `20260527000005_lead_raw_payloads_error.sql` | `ingestion_error` column on `lead_raw_payloads` |
| `20260527000006_scratchpad_rls.sql` | RLS fix for `private_scratchpad` |
| `20260527000007_round_robin_fn.sql` | Atomic `get_next_round_robin_agent()` DB function |
| `20260527000008_lead_dedup.sql` | `previous_lead_id` FK; `get_active_lead_by_phone()` (v1 — `SETOF leads`, `SELECT *`) |
| `20260608000090_fix_select_star_rpcs.sql` | `get_active_lead_by_phone` v2 — `RETURNS TABLE` with 9 explicit columns; no `SELECT *`. `get_personal_tasks` unchanged |
| `20260527000009_lead_personal_details.sql` | `personal_details JSONB` on `leads` |
| `20260528000010_lead_filter_indexes.sql` | UTM source/campaign/outcome partial indexes |
| `20260528000011_lead_search_index.sql` | `text_pattern_ops` index for ILIKE search |
| `20260528000012_ad_creatives.sql` | `ad_creatives` table |
| `20260528000013_performance_indexes.sql` | Performance page partial indexes |
| `20260528000014_campaign_analytics.sql` | `get_campaign_metrics` RPC + campaign indexes |
| `20260528000015_campaign_detail_metrics.sql` | `get_campaign_detail_metrics` RPC |
| `20260528000016_notifications.sql` | `notifications` table; Realtime enabled |
| `20260528000017_os_tasks.sql` | `task_groups` table; `task_messages` table (append-only + Realtime); `tasks` extended (title, description, priority, task_category, group_id, new status enum); `notifications` type CHECK expanded with `task_assigned` |
| `20260528000018_task_groups_rls_domain.sql` | A-09 fix: `task_groups` SELECT and UPDATE policies now enforce `get_user_domain() = domain` for manager role; admin/founder retain full access |
| `20260528000019_task_messages_rls_creator.sql` | A-09 fix: `task_messages` SELECT and INSERT policies add `created_by = auth.uid()` (task creator was locked out of own chat) and domain-scope managers via `task_groups.domain` join |
| `20260528000020_group_task_summaries_rpc.sql` | `get_group_task_summary` RPC |
| `20260528000021_task_suppression_audit.sql` | `task_messages` suppression columns (`is_suppressed`, `suppressed_by`, `suppressed_at`) + UPDATE RLS for admin/founder; `task_audit_log` append-only table + `log_task_changes()` trigger (AFTER UPDATE on tasks, six fields: title/description/status/priority/due_at/assigned_to) |
| `20260529000022_task_remarks.sql` | DROP TABLE task_messages CASCADE (pre-production, no data); CREATE TABLE `task_remarks` (replaces task_messages with added `status_change` nullable column, ASC index, same suppression + RLS pattern); Realtime enabled on `task_remarks` |
| `20260529000023_task_attachments.sql` | ADD COLUMN `attachments jsonb NOT NULL DEFAULT '[]'` to `tasks`; CHECK constraint `tasks_attachments_is_array` validates JSON array; intentionally excluded from `log_task_changes()` trigger |
| `20260529000029_get_dashboard_summary.sql` | `get_dashboard_summary(p_role, p_domain, p_user_id)` RPC — single jsonb response with 4 keys (`agent_tasks`, `agent_activity`, `lead_status`, `campaigns`); SECURITY DEFINER; role-based CTEs; all COUNT cast `::int`; GRANT EXECUTE to authenticated |
| `20260529000030_rpc_add_lead_call_note.sql` | `add_lead_call_note(p_lead_id, p_author_id, p_content, p_call_outcome, p_now)` RPC — wraps all DB writes for `addLeadCallNote` action in one transaction (note insert + lead UPDATE + 2–3 activity inserts); returns `jsonb` with `note_id`, `new_call_count`, `did_auto_advance`, `assigned_to`, `domain`, `old_status`; SECURITY DEFINER; access control stays in action layer |
| `20260529000031_rpc_update_lead_status.sql` | `update_lead_status(p_lead_id, p_actor_id, p_status, p_reason, p_now)` RPC — wraps all DB writes for `updateLeadStatus` action in one transaction (lead UPDATE + activity insert + conditional nurturing task + task_gia_meta); returns `jsonb` with `changed`, `old_status`, `new_status`, `assigned_to`, `domain`, `first_name`, `last_name`; early-returns `{ changed: false }` when status unchanged; SECURITY DEFINER; access control and won notifications stay in action layer |
| `20260530000032_whatsapp_conversations.sql` | `whatsapp_conversations` table — one row per lead/phone; `wa_id` (E.164 without +) and `lead_id` both UNIQUE; `bot_active/bot_paused_by/bot_paused_at` columns; `can_access_wa_conversation()` SECURITY DEFINER helper; RLS mirrors leads table (agent → assigned leads, manager → domain, admin/founder → all); Realtime enabled |
| `20260530000033_whatsapp_messages.sql` | `whatsapp_messages` table — **append-only** with one narrow UPDATE exception (delivery receipts: `status` + `status_at` only, via service-role client); `wa_message_id` is a **partial unique index** (`WHERE wa_message_id IS NOT NULL`) to allow multiple NULL rows for optimistic inserts; same RLS domain-scoping via `can_access_wa_conversation()`; no DELETE policy ever; Realtime enabled |
| `20260530000034_whatsapp_reads.sql` | `whatsapp_conversation_reads` table — per-agent read position for unread badge counts (not in The_Gia.md §14 — deliberate addition); UNIQUE(conversation_id, agent_id); RLS: agents read/insert/update own rows only; no other roles need policies |
| `20260530000035_rpc_add_task_remark_with_status.sql` | `add_task_remark_with_status(p_task_id, p_author_id, p_content, p_status_change)` RPC — collapses remark + optional status UPDATE into 1 round-trip; SECURITY DEFINER; access control stays in action layer (view = post via user-scoped `tasks` SELECT before `adminClient.rpc()`) |
| `20260529000024_task_tags.sql` | `tags text[] NOT NULL DEFAULT '{}'` on `tasks`; GIN index for personal tasks |
| `20260529000025_task_performance_indexes.sql` | Task index fix + `get_personal_tasks` v1 |
| `20260529000026_get_personal_tasks_cursor.sql` | Cursor params on `get_personal_tasks` |
| `20260529000027_lead_sla_columns.sql` | `status_changed_at`, `last_activity_at` on `leads` |
| `20260529000028_lead_sla_timers.sql` | `lead_sla_timers` table |
| `20260529000040_rpc_add_lead_plain_note.sql` | `add_lead_plain_note` RPC — plain note without call_count |
| `20260531000049_leads_deal_duration.sql` | `leads.deal_duration` for won leads |
| `20260531000052_get_deals_summary.sql` | `get_deals_summary` RPC — Deals page summary strip aggregates |
| `20260531000053_get_deals_summary_manager_domain_fix.sql` | `get_deals_summary` — `p_caller_domain` vs `p_filter_domain` split for manager role-gate |
| `20260531000057_task_type_other.sql` | `task_type` vocabulary → `call`, `whatsapp_message`, `other`; backfill legacy values; nurturing auto-task uses `other` |
| `20260606000080_get_leads_status_counts.sql` | `get_leads_status_counts(p_agent_id, p_date_from, p_date_to, p_campaign, p_search, p_health, p_source, p_outcomes, p_statuses)` RPC — returns `TABLE(status text, cnt bigint)` for the full filtered dataset; role/domain scoping self-enforced via `get_user_role()` / `get_user_domain()`; STABLE SECURITY DEFINER; empty-array guard on `p_outcomes`/`p_statuses` (treats as "no filter"); GRANT EXECUTE to authenticated. Called in `Promise.all` alongside the paginated query in `getLeadsByRole`. **Superseded by 0083 — `p_health` removed.** |
| `20260608000083_status_counts_drop_health.sql` | Removes the `lead_health` feature remnant from `get_leads_status_counts`. Drops the old 9-param overload (with `p_health`) and recreates the 8-param version without `p_health` / the `l.lead_health` predicate — the `lead_health` column was dropped in 0082. Matches the updated `getLeadsByRole` / `getLeadsForExport` call sites (8 RPC params). STABLE SECURITY DEFINER; GRANT EXECUTE to authenticated. |
| `20260608000084_lead_health_full_removal.sql` | Idempotent completion of `lead_health` removal on production: `DROP COLUMN IF EXISTS lead_health`, `DROP INDEX IF EXISTS idx_leads_health`, `DROP FUNCTION IF EXISTS refresh_lead_health_bulk()`, `CREATE OR REPLACE` for `add_lead_call_note` and `add_lead_plain_note` with `lead_health` SET clauses removed (bodies match 0030/0040). Safe when 0082 already ran. |
| `20260608000092_fix_ad_creatives_storage_rls.sql` | Storage RLS for the `ad-creatives` bucket. Drops legacy permissive `"Ad Creative Modal insert/delete"` policies; creates `ad_creatives_storage_insert` / `ad_creatives_storage_delete` gating `storage.objects` writes (`bucket_id = 'ad-creatives'`) to `profiles.role IN ('admin','founder')`. No SELECT policy — bucket read stays public for dossier/campaign playback. Mirrors the table-level write RLS from 0012. |
| `20260608000094_explicit_insert_delete_policies.sql` | `tasks_insert` (personal, self-assigned); `tasks_delete` (agent, personal, non-terminal); `tasks_delete_privileged` (manager/admin/founder). No deals INSERT/DELETE policies — `COMMENT ON TABLE public.deals` documents intentional RPC/service-role-only writes. |
| `20260604000069_dashboard_date_filter.sql` | Extends `get_dashboard_summary` (5th + 6th params: `p_date_from timestamptz DEFAULT NULL`, `p_date_to timestamptz DEFAULT NULL`), `get_lead_pipeline_refresh` (same two params), and `get_campaign_pipeline_refresh` (same two params). Date filter applies `created_at >= p_date_from AND created_at < p_date_to` to the `lead_status` and `campaigns` CTEs only. `agent_tasks` and `agent_activity` are unaffected. Backwards-compatible: all existing callers that omit the params see NULL → all-time behaviour unchanged. All three functions remain STABLE SECURITY DEFINER; GRANT EXECUTE preserved. |
| `20260604000070_fix_pipeline_agent_total.sql` | Fixes `agent_counts` / `campaign_agg` totals: `COUNT(*)` on status subquery rows → `SUM(cnt)` (actual lead count). Affects `get_dashboard_summary`, `get_lead_pipeline_refresh`, `get_campaign_pipeline_refresh`. Restores correct per-agent stacked bar proportions and campaign sort order. |
| `20260530000036_rpc_get_wa_unread_count.sql` | `get_wa_unread_count()` RPC — per-agent unread WhatsApp conversation count; LEFT JOIN `whatsapp_conversation_reads` on `agent_id = auth.uid()`; counts open conversations where `last_read_at IS NULL` or `last_message_at > last_read_at`; gated by `can_access_wa_conversation()`; RETURNS integer; STABLE SECURITY DEFINER; GRANT EXECUTE to authenticated |
| `20260530000037_whatsapp_messages_outbound_insert.sql` | `wa_messages_outbound_insert` RLS INSERT policy — allows authenticated agents/managers/admin/founder to insert outbound rows (`direction='outbound'`, `sender_type='agent'`, `sender_id=auth.uid()`, `can_access_wa_conversation(lead_id)`); inbound inserts remain service-role only via `createAdminClient()` in `whatsapp-ingestion.ts` |
| `20260530000039_fix_nurturing_task_insert.sql` | `CREATE OR REPLACE FUNCTION update_lead_status` — fixes nurturing auto-task: original RPC omitted `title` (NOT NULL after migration 0017) and `task_category` (must be `'gia_followup'`, not the default `'personal'`); both omissions caused the INSERT to fail silently; task now gets title `'Nurturing follow-up'` and correct category |
| `20260530000041_normalize_lead_domain.sql` | Single-transaction domain normalization (7 steps): (1) UPDATE profiles agent rows concierge→onboarding; (2) UPDATE leads for all indulge*\*/concierge→enum values; (3) UPDATE whatsapp_notification_logs.domain; (4) DO block audits both tables, RAISE WARNING + remap any unexpected value; (5) DROP all 15 RLS policies that reference leads.domain or task_groups.domain (direct or via sub-SELECT), including lead_activities_select, lead_notes_select, `lead_sla_timers*_*select`, `wa_conversations*_`, `wa*messages*\*`; (6) ALTER TABLE leads/task_groups/whatsapp_notification_logs domain TYPE app_domain; (7) RECREATE all 15 dropped policies + CREATE OR REPLACE can_access_wa_conversation() — all `::text`casts on`get_user_domain()`removed since both sides are now`app_domain` |
| `20260530000042_fix_group_task_summaries_domain_type.sql` | `CREATE OR REPLACE FUNCTION get_group_task_summaries` — fixes `42883` runtime error caused by migration 0041 changing `task_groups.domain` to `app_domain` while the RPC still compared `tg.domain = get_user_domain()::text` (no `=(app_domain, text)` operator exists); both sides are now `app_domain` so the `::text` cast is removed; `tg.domain::text` cast added to the SELECT list to preserve the `RETURNS TABLE (domain text)` contract |
| `20260530000043_fix_dashboard_summary_domain_type.sql` | `CREATE OR REPLACE FUNCTION get_dashboard_summary` — fixes `42883` runtime error on `/dashboard` caused by migration 0041 changing `leads.domain` to `app_domain` while `p_domain` parameter was still `text`; parameter type changed to `app_domain`; old `(text, text, uuid)` overload dropped |
| `20260530000044_fix_campaign_metrics_domain_type.sql` | `CREATE OR REPLACE FUNCTION get_campaign_metrics` — fixes `42883` runtime error on `/campaigns` caused by migration 0041 changing `leads.domain` to `app_domain` while `p_domain` parameter was still `text`; parameter type changed to `app_domain`; `domain::text` cast added to SELECT list to preserve `RETURNS TABLE (domain text)` contract; old `(text, timestamptz, timestamptz)` overload dropped |
| `20260530000045_lead_slug.sql` | `leads.slug text UNIQUE` column; `generate_lead_slug(first_name, last_name, phone)` function; `set_lead_slug()` trigger (BEFORE INSERT, immutable — no UPDATE trigger); `idx_leads_slug` partial unique index (`WHERE slug IS NOT NULL`); back-fills all existing rows with non-null phone |
| `20260530000046_lead_slug_collision_fix.sql` | Replaces `generate_lead_slug` with a collision-safe version that appends `-2`, `-3`, ... until the slug is free (archived/previous leads can share name+phone-suffix); re-runs backfill via per-row DO loop ordered `created_at ASC` so oldest lead keeps the clean slug |
| `20260531000048_dashboard_activity_limit_25.sql` | `CREATE OR REPLACE FUNCTION get_dashboard_summary` — bumps `agent_activity` LIMIT 10 → 25; all other CTEs unchanged; `getAgentRecentActivity` service function limit also bumped to 25 |
| `20260531000051_task_remark_rpc_auth_fix.sql` | `add_task_remark_with_status` — removes broken `auth.uid()` gate (NULL under service-role); `tasks_agent_select` + `task_remarks` RLS aligned to creator/assignee; action gates on user-scoped `tasks` SELECT (view = post) |
| `20260531000050_dashboard_activity_role_scoped.sql` | `CREATE OR REPLACE FUNCTION get_dashboard_summary` — fixes `agent_activity` CTE: admin/founder now see all activities (no filter); manager sees activities on leads in their domain; agent still sees `actor_id = p_user_id` only |
| `20260531000047_dashboard_agent_tasks_all_categories.sql` | `CREATE OR REPLACE FUNCTION get_dashboard_summary` — replaces `agent_tasks` CTE: now fetches all 3 task categories (`personal`, `group_subtask`, `gia_followup`) with active statuses (`to_do`, `in_progress`, `in_review`); LEFT JOINs `task_gia_meta`+`leads` for gia context label and `task_groups` for group label; sorted overdue→priority→due_at; limit 30; `newLeadsCount` removed |
| `20260531000054_create_lead_gia_task.sql` | `create_lead_gia_task(p_lead_id, p_assigned_to, p_created_by, p_task_type, p_title, p_description, p_priority, p_due_at)` RPC — two-INSERT transaction (`tasks` + `task_gia_meta`) to prevent orphaned task rows; returns the full `tasks` row; SECURITY DEFINER; GRANT EXECUTE to authenticated |
| `20260531000055_get_gia_tasks.sql` | `get_gia_tasks(p_user_id uuid, p_role text, p_domain app_domain)` RPC — returns all `gia_followup` tasks for the caller with joined lead identity (`lead_id`, `lead_first_name`, `lead_last_name`, `lead_phone`, `lead_slug`, `lead_domain`); agent role filters `assigned_to = p_user_id`; other roles filter `leads.domain = p_domain`; `p_domain` typed `app_domain` to avoid `42883` after migration 0041; order: active tasks first, then `due_at ASC NULLS LAST`; STABLE SECURITY DEFINER; GRANT EXECUTE to authenticated; includes `ADD COLUMN IF NOT EXISTS slug` guard |
| `20260531000056_get_gia_tasks_slug_prereq.sql` | Hotfix when 0055 ran before 0045: ensures `leads.slug` + `idx_leads_slug`, optional backfill via `generate_lead_slug`, recreates `get_gia_tasks` (fixes `42703 column l.slug does not exist`) |
| `20260601000058_ad_creatives_multi_video.sql` | Drops the UNIQUE constraint on `ad_creatives.campaign_key` so one campaign can have multiple ad videos (one row per video). Idempotent `pg_constraint` guard; recreates non-unique `idx_ad_creatives_campaign_key`; normalisation CHECK from 0012 unchanged. |
| `20260602000059_agent_shift_days.sql` | `ADD COLUMN IF NOT EXISTS shift_days integer[]` on `agent_routing_config`; JS day-of-week array (0=Sun…6=Sat); NULL = use global BUSINESS_HOURS. |
| `20260602000060_leads_resolution_reason.sql` | `leads.resolution_reason TEXT` column; partial index `idx_leads_resolution_reason` (junk/lost, not archived); `CREATE OR REPLACE FUNCTION update_lead_status` — surgically adds persistence of `p_reason` to the column on junk/lost, clears it on revive (in_discussion); `GRANT EXECUTE` preserved. |
| `20260603000062_get_dashboard_summary_role_branch.sql` | `DROP FUNCTION get_dashboard_summary(text, app_domain, uuid)` then `CREATE OR REPLACE` as 4-param `(text, app_domain, uuid, app_domain DEFAULT NULL)`: agent role returns immediately after agent_tasks + agent_activity CTEs (empty stubs for lead_status/campaigns); manager/admin/founder compute all 4 CTEs; domain scoping: manager → p_domain, admin/founder + p_initial_domain → that domain, admin/founder + NULL → all-org. Only one overload remains. |
| `20260603000063_get_agent_recent_activity.sql` | New `get_agent_recent_activity(p_role text, p_domain app_domain, p_user_id uuid)` RPC — single `lead_activities LEFT JOIN leads` query; CASE role filter (admin/founder: all, manager: domain-scoped, agent: actor_id); returns jsonb array of 25 activities DESC; STABLE SECURITY DEFINER; GRANT EXECUTE to authenticated. Replaces two-step Node.js pump in `getAgentRecentActivity`. |
| `20260603000064_dashboard_refresh_rpcs.sql` | `get_lead_pipeline_refresh(p_role text, p_domain app_domain)` — STABLE SECURITY DEFINER; returns `{totals, byAgent}` jsonb matching `DashboardLeadStatusSummary`; manager → domain-scoped, admin/founder → all. `get_campaign_pipeline_refresh(p_role text, p_domain app_domain)` — same scoping; returns top-12 campaign mix array matching `DashboardCampaignStatusMix[]`. Both eliminate Node-side aggregation from `getLeadStatusSummary` and `getLeadsByCampaign`. GRANT EXECUTE to authenticated. |
| `20260530000038_whatsapp_notification_logs.sql` | `whatsapp_notification_logs` table — one row per outbound template send attempt. Stores last-4 phone digits only (never full numbers), `gupshup_status`/`gupshup_body`/`delivered`. `type` CHECK initially `('agent_assignment','founder_alert')` — later widened by 0067. RLS enabled. |
| `20260530000040_rpc_add_lead_plain_note.sql` | `add_lead_plain_note(p_lead_id, p_author_id, p_content, p_now)` RPC — plain note insert + `last_activity_at` bump, **no** `call_count`/`last_call_outcome` change. SECURITY DEFINER; backs `addLeadNote`. |
| `20260602000061_remove_private_scratchpad.sql` | `DROP COLUMN leads.private_scratchpad`; `DROP FUNCTION get_lead_scratchpad(uuid)` — scratchpad feature removed. |
| `20260603000065_attribution_refactor.sql` | Attribution refactor: 7 flat ad columns → `source`, `medium`, `utm_campaign` + `attribution jsonb`. Renames `utm_source→source`, `utm_medium→medium`; drops `platform`/`campaign_id`/`ad_name`/`utm_content` (backfilled into `attribution` JSONB). Filter index renamed `idx_leads_utm_source → idx_leads_source`. |
| `20260603000066_leads_city_column.sql` | `leads.city text` top-level column; backfills out of `personal_details->>'city'` then strips the key from the JSONB. |
| `20260603000067_extend_whatsapp_notification_log_types.sql` | Widens `whatsapp_notification_logs.type` CHECK to `('agent_assignment','founder_alert','sla_breach','lead_initiation')`. Historical rows unchanged (SLA rows stay `agent_assignment` — no reliable discriminator). |
| `20260604000066_domain_health_metrics.sql` | `get_domain_health_metrics(p_domains app_domain[], p_date_from, p_date_to)` RPC — one row per requested domain (always, even zero leads); CTEs for cohort/closures/pipeline/calls; pipeline is a live snapshot, rest date-filtered. STABLE SECURITY DEFINER. **NOTE: duplicate `0066` timestamp prefix shared with `leads_city_column` — both apply, ordered by full filename.** |
| `20260604000068_domain_health_add_calls_revenue.sql` | Extends `get_domain_health_metrics` with `total_calls_made` (SUM call_count on period cohort) and `total_revenue` (SUM deal_amount on `status='won'` by `status_changed_at`). Later repointed to `deals` in 0076. |
| `20260605000058_task_groups_flat_visibility.sql` | **Flat group-task visibility** (replaces role-branched model): a user sees groups they CREATED or are ASSIGNED a subtask in; delete = creator only. `get_group_task_summaries` updated with the same two-condition WHERE; all `get_user_role()`/`get_user_domain()` removed. Adds `idx_tasks_group_assignee`. **NOTE: duplicate `0058` prefix shared with `ad_creatives_multi_video`.** |
| `20260605000071_avatars_storage_bucket.sql` | Public `avatars` storage bucket; object name == `profile.id`; idempotent RLS: `avatars_public_read` + own-object insert/update/delete. |
| `20260605000072_create_deals_table.sql` | `public.deals` first-class table (reverses the 2026-05-31 "no deals table" decision). Columns: `lead_id` (nullable — walk-ins), `client_id` (FK deferred), contact fields, `domain`, `deal_amount`, `deal_type` (`membership`/`retail`), `deal_duration`, `assigned_to`, `won_at`, `archived_at`. RLS mirrors leads scoping. No INSERT/DELETE policies (RPC/service-role writes only). |
| `20260605000073_backfill_deals_from_won_leads.sql` | Idempotent backfill (`NOT EXISTS` guard) of `public.deals` from existing `status='won'` leads — every pre-migration `/deals` row stays visible. |
| `20260605000074_get_deals_summary_over_deals.sql` | Rewrites `get_deals_summary` to query `public.deals` (was `leads`); structural WHERE collapses to `archived_at IS NULL`; date filters apply to `won_at`. Two-domain param split (`p_caller_domain`/`p_filter_domain`) preserved from 0053. |
| `20260605000075_deals_add_source.sql` | `deals.source text` column (CHECK `meta`/`google`/`website`/`whatsapp`/`referral`/`ypo`/`events`) mirroring `leads.source`; nullable; partial index `deals_source_idx`. |
| `20260605000076_domain_health_revenue_from_deals.sql` | Repoints `get_domain_health_metrics.total_revenue` to `SUM(deals.deal_amount) WHERE archived_at IS NULL` by `won_at`. All other CTEs stay on `leads` (lifecycle, not revenue). |
| `20260606000077_lead_health_column.sql` | **(reverted by 0082)** Adds `leads.lead_health text` (`healthy`/`needs_attention`/`at_risk`) + `idx_leads_health`. |
| `20260606000078_lead_health_rpc_hooks.sql` | **(reverted by 0082)** Patches `add_lead_call_note`/`add_lead_plain_note`/`update_lead_status` to set `lead_health` on every write. |
| `20260606000079_refresh_lead_health_rpc.sql` | **(reverted by 0082)** `refresh_lead_health_bulk()` RPC — hourly bulk health recompute over active non-terminal leads; SECURITY DEFINER. |
| `20260606000081_dashboard_cold_leads.sql` | Adds `cold_leads_count` CTE to `get_dashboard_summary` (no activity 5+ days, non-terminal; threshold matches `COLD_LEAD_THRESHOLD_DAYS`). manager → domain, admin/founder → all, agent early-returns. Backs `ManagerColdLeadsWidget` first-paint. |
| `20260606000082_revert_lead_health.sql` | **Reverts the entire `lead_health` feature (077–079):** drops the column, `idx_leads_health`, and `refresh_lead_health_bulk()`; restores the three RPC bodies to their pre-health form (matching 0030/0040/0060). |
| `20260608000085_fix_wa_unread_count.sql` | Fixes `get_wa_unread_count()` — `can_access_wa_conversation()` expects `lead_id`, not `wc.id`; the wrong arg made the unread badge return 0 for every agent. |
| `20260608000086_fix_tasks_status_default.sql` | `ALTER tasks.status SET DEFAULT 'to_do'` — 0017 migrated the CHECK values but left the default as legacy `'pending'`, violating `tasks_status_check` on INSERT. |
| `20260608000087_fix_campaign_first_touch_key.sql` | Fixes `get_campaign_detail_metrics.avg_hours_to_first_touch` — lateral join read `details->>'to'` but `update_lead_status` writes `old_status`/`new_status` keys. |
| `20260608000088_rls_initplan_hoist.sql` | Hoists `get_user_role()`/`get_user_domain()` into uncorrelated `(SELECT …)` scalar subqueries across RLS policies (leads + others) so STABLE helpers evaluate once per statement, not per row. (Three policies missed here are fixed in 0095.) |
| `20260608000089_drop_dead_rpc_overloads.sql` | Drops superseded overloads: old 4-param (no-date) `get_dashboard_summary` and old 2-param `get_campaign_pipeline_refresh`. |
| `20260608000091_fix_leads_update_policy.sql` | `leads_update` RLS now requires `archived_at IS NULL` — archived leads are immutable via direct UPDATE; un-archive must go through a SECURITY DEFINER path. |
| `20260608000093_remove_duplicate_avatar_policies.sql` | Drops the legacy quoted-name avatar storage policies; canonical set from 0071 (`avatars_*`) remains. |
| `20260608000095_rls_hoist_missed_three.sql` | Completes the 0088 InitPlan hoist for three policies it missed: `wa_notif_logs_admin_founder_select`, `routing_config_update`, and one more — wraps bare `get_user_role()` in `(SELECT …)`. |
| `20260608000096_attribution_comment.sql` | `COMMENT ON COLUMN leads.attribution` documenting the immutable-after-insert UTM/platform snapshot contract. No data change. |
| `20260608000097_drop_leads_dead_deal_columns.sql` | Drops orphaned `leads.deal_amount`/`deal_type`/`deal_duration` (and CASCADE their CHECK constraints) — dead since deals became first-class (0072–0074); `recordDeal` writes to `public.deals`. |
| `20260611000098_leads_search_text_trgm.sql` | **(perf C-2)** `pg_trgm` extension (schema `extensions`); `leads.search_text` STORED generated column (`first_name + last_name + email + city + phone`) — THE search surface for every lead search path; `idx_leads_search_trgm` GIN trigram partial index (`archived_at IS NULL`). Table rewrite on ADD COLUMN — fast at current volume. Bonus: multi-word name search now matches across the first/last boundary. |
| `20260611000099_status_counts_total_fold.sql` | **(perf C-1)** `get_leads_status_counts` v3 (10 params): adds `p_domain` (admin/founder Gia slice only — agent/manager scoping stays self-derived) + `p_going_cold` (fixes the live PGRST202 — the service had passed `p_going_cold` since the going-cold preset shipped but no overload had it, so EVERY list load's count RPC failed and pills rendered empty); search switched to `l.search_text ILIKE`; `p_date_to` now inclusive (`<=`, matches `.lte()`). Drops the 8-param overload. `totalCount` in `getLeadsByRole` is now the sum of these rows — the paginated query dropped `{ count: 'exact' }`. **Run before deploying the service change** (new signature is a superset with defaults; old deployed call matches immediately). |
| `20260611000100_leads_domain_created_index.sql` | **(perf C-4)** `idx_leads_domain_created (domain, created_at DESC) WHERE archived_at IS NULL` — serves the manager list path (`domain = X ORDER BY created_at DESC LIMIT 30`) in one descent. Verify with `EXPLAIN ANALYZE` post-deploy; cheap to drop if unused. |
| `20260611000101_agent_performance_rpcs.sql` | **(perf D-2)** `_agent_core_metrics(agent, from, to)` internal helper (EXECUTE revoked from clients); `get_agent_performance(from, to, prev_from?, prev_to?)` — SELF-SCOPED (`auth.uid()` + `get_user_domain()`, no identity params) jsonb: core + previous + effort + outcomes + benchmarks; replaces the agent view's ~17-query fan-out. Benchmarks now true domain-wide (SECURITY DEFINER) — previously agent-RLS silently reduced them to the caller's own rows. `get_agent_roster_performance(from, to, domain?)` — role-gated (manager+ only; manager pinned to `get_user_domain()`, p_domain honoured only for admin/founder, NULL = all); one row per active agent via LEFT-JOINed aggregate CTEs (leads cohort, won/lost, deals revenue, first-touch AVG). Both STABLE SECURITY DEFINER `SET search_path = public`; GRANT to authenticated. |
| `20260611000102_revoke_scope_param_rpcs.sql` | **(security F-1, Q-13)** REVOKEs EXECUTE from `PUBLIC, anon, authenticated` on all 11 Class B/C read-RPC signatures that trust caller-supplied scope params or have no internal gate: `get_dashboard_summary`, `get_agent_recent_activity`, **both** `get_lead_pipeline_refresh` overloads (the 2-param one is dead code 0089 missed), `get_campaign_pipeline_refresh`, `get_deals_summary`, `get_gia_tasks`, `get_campaign_metrics`, `get_campaign_detail_metrics`, `get_campaign_agent_distribution`, `get_domain_health_metrics`. Explicit `service_role` GRANTs. The 10 service call sites switched to `createAdminClient()` — args stay session-derived (caller = trust boundary). Self-scoped RPCs keep their `authenticated` GRANT. |
| `20260611000103_leads_update_explicit_with_check.sql` | **(security §1 polish)** `leads_update` policy recreated with an explicit `WITH CHECK` identical to its `USING` body (0091 form, InitPlan hoist preserved) — previously the new-row gate was implicit via PostgreSQL's USING-fallback on UPDATE. Zero behaviour change; survives a future column-specific `WITH CHECK` edit. |
| `20260612000104_ad_spend_daily.sql` | `ad_spend_daily` table — day-grain Meta spend (budget page); `UNIQUE(campaign_key, spend_date, source)`; `campaign_key` lowercase+trim CHECK (mirrors ad_creatives 0012 — the lead join depends on it); RLS manager+ read, admin/founder write; reuses `update_updated_at()`. |
| `20260612000105_domain_targets.sql` | `domain_targets` table — founder-set monthly targets per domain; metric CHECK `('deals_closed')`, period CHECK `('month')`, `UNIQUE(domain, metric, period)`; RLS all-authenticated read, admin/founder write. Nothing seeded. |
| `20260612000106_get_budget_summary.sql` | `get_budget_summary(p_date_from, p_date_to)` RPC — spend per campaign LEFT-joined to lead counts (`created_at` cohort, `lower(trim(utm_campaign))` join) + deals (count/revenue by `won_at` via `deals.lead_id`); spend dates filtered on IST calendar days. STABLE SECURITY DEFINER; **EXECUTE revoked from authenticated (Q-13)** — admin client only. |
| `20260612000107_domain_health_deals_count.sql` | Extends `get_domain_health_metrics` with `total_deals` (COUNT from `public.deals` by `won_at`, same source/date-field as `total_revenue`); all other CTEs unchanged; re-applies the 0102 revoke posture after DROP/recreate. |
| `20260612000108_agent_today_pulse.sql` | `get_agent_today_pulse(p_today_start, p_date_from, p_date_to)` RPC — SELF-SCOPED (`auth.uid()`, GRANT authenticated, 0101 pattern): calls-today new-vs-old split (partition of one row set — always sums to total), 14-day daily call trend (IST day boundary passed in from `lib/utils/ist`), period deals count + revenue from `public.deals`. |
| `20260612000109_leads_service_interests.sql` | **(Call Intelligence Phase 1)** `leads.service_interests text[] NOT NULL DEFAULT '{}'` + partial GIN index (`archived_at IS NULL`). `text[]` by design, never the category enum — per-domain vocabularies live in `lib/constants/interests.ts`. Spec named this 0085/0086 — renumbered, those slots were taken. |
| `20260612000110_call_intelligence_tables.sql` | **(Call Intelligence Phase 1)** `service_cases` (brag library: text category, freeform tags incl. mandatory city slug, weighted generated FTS vector + GIN, tags GIN, featured/sort indexes, `update_updated_at()` trigger, dormant `embedding extensions.vector(1536)` — **HNSW index deferred to Phase 2**) + `conversation_hooks`. RLS both tables: all-authenticated SELECT; admin/founder-only writes via InitPlan-hoisted `(SELECT get_user_role())`. Installs the `vector` extension into `extensions`; adds `public.immutable_array_to_string(text[], text)` (plain `array_to_string` is STABLE — Postgres rejects it in a GENERATED column). **Content:** the migration seeds nothing; the verified library (150 cases + 30 hooks, `onboarding`) was bulk-seeded 2026-06-12 via `scripts/seed-call-intelligence.ts` — subsequent edits go via the admin path only. |
| `20260612000111_sla_policies.sql` | **(Follow-up engine Phase 2)** `sla_policies` config table — one row per engine rule: `trigger_kind` CHECK (`status`/`outcome`/`task_due`), `trigger_value`, `threshold_minutes`, `recipient_role` CHECK (`agent`/`manager`/`founder`), `auto_task`, `channels text[]`, `hours_mode` CHECK (`agent_shift`/`business`/`clock`), `active`. RLS: admin/founder SELECT only; writes service-role (future settings UI goes through an admin-gated action). Seeded: the 8 live SLA rules copied from `SLA_RULES` (statusTrigger `active` stored as the real status `nurturing`) + `SLA-01C` (new·45min·founder) + `CAD-01A/B/C` (outcome cadence: rnr/switched_off/wrong_number) + `TASK-01A/B` (gia task due / overdue+30 clock-min). Engine reads per job run — never module-cached. |
| `20260612000112_last_call_outcome_at.sql` | `leads.last_call_outcome_at timestamptz` — when the latest outcome was logged (the cadence 7-day freshness window reads it; `last_call_outcome` itself has existed since 0003). `CREATE OR REPLACE add_lead_call_note` — body identical to 0084 plus the one new SET line. Backfill from latest outcome-bearing `lead_notes.created_at`; leads whose outcome predates note history stay NULL = never fresh = never arm. |
| `20260612000113_task_overdue_and_notification_types.sql` | `tasks.overdue_at timestamptz` + partial index — stamped **exactly once** by `check-task-overdue` (`UPDATE … WHERE overdue_at IS NULL`); deliberately a timestamp, the `tasks.status` CHECK does NOT grow. Widens `notifications.type` CHECK with `sla_breach_founder` + `task_overdue_manager` and `whatsapp_notification_logs.type` CHECK with `task_due_reminder` + `task_overdue_manager`. |
| `20260612000114_cad02_in_discussion_cadence.sql` | **(Follow-up engine Phase 3)** Seeds `CAD-02A` — the In Discussion 48h cadence (`status` · `in_discussion` · 2880 biz-min · agent · auto_task · channels `{}` · `agent_shift`); idempotent `ON CONFLICT (code) DO NOTHING`. Paired engine extension: every CAD-prefixed code takes the cadence fire path (follow-up task + re-arm) regardless of trigger_kind; status cadences re-arm `threshold_minutes` ahead. **⚠️ NOT yet applied to prod.** |
| `20260612000115_dashboard_agent_snapshot_counts.sql` | **(Agent dashboard redesign)** `get_dashboard_summary` v9 — adds `pending_calls_count` (open `gia_followup` tasks for the caller) + `new_leads_count` (own leads at `new`, not archived), computed in the agent early-return branch only (`0` stubs for manager+); **zero date inputs** — live pipeline snapshots, never cohort-scoped. Also restores the 0070 `SUM(cnt)` totals in `agent_counts`/`campaign_agg` (0081 had regressed both to `COUNT(*)` — status-row counts, not lead counts). Signature unchanged; re-states the 0102 revoke posture (verified post-apply: `authenticated` cannot EXECUTE, `service_role` can). **Applied to prod + ledger-recorded 2026-06-12.** |
| `20260612000116_elaya_foundation.sql` | **(Elaya foundation)** Six tables: `elaya_conversations` (24h session window — expiry enforced in elaya-service, not SQL), `elaya_messages` (**append-only A-11** — no UPDATE/DELETE ever; `channel` column + denormalised `sender_id` with a partial `(sender_id, created_at) WHERE role='user'` index serving the daily cap), `user_context` (service-role writes only), `elaya_actions` (**empty until Phase 2** — reserved for action proposals), `llm_providers` (job-type → provider+model; seeded routing=haiku-4-5 / reasoning=sonnet-4-6; read per request like `sla_policies`), `elaya_settings` (key/value; seeded cap 200 / masking 'light' / expiry 24h). RLS all six: own-row SELECTs (messages via conversation EXISTS); user-role message INSERT own-conversation only; assistant/context/action/config writes service-role; config SELECT admin/founder. Reuses `update_updated_at()`. **⚠️ NOT yet applied to prod.** Regenerate `database.ts` after applying (interim types: `src/lib/types/elaya.ts`). |
| `20260614000119_lead_revival.sql` | **(Lead Revival Phase R1)** Two tables. `revival_policies` — config (the `sla_policies` pattern): PK `trigger_status` CHECK (`touched`/`in_discussion`/`nurturing` — **cold excluded by design**), `silence_days`, `daily_cap_per_agent` (default 25), `active`; `update_updated_at` trigger; RLS admin/founder SELECT only, service-role writes; read per sweep run. Seeded touched=60d / in_discussion=60d / nurturing=90d / cap=25. `revival_candidates` — the candidate ledger: `lead_id`, **denormalised `assigned_to`** (so the daily-cap count is a native `.eq('assigned_to')` filter, NOT a silently-dropped PostgREST embed filter on a `head:true`/count query — the `getNextLeadTask` caveat), `verdict` CHECK (`revive`/`unsure`/`dismiss` — the gate's three-way call; `dismiss` is the VERDICT, `dismissed` is the candidate STATUS it writes), `ai_reasoning`, `status` CHECK (`open`/`actioned`/`dismissed`), `trigger_status`, `suggested_revive_at`, `resolved_at`/`resolved_by`. **A-11 carve-out** (the `elaya_actions` precedent): SELECT-only RLS scoped by role/domain via an `EXISTS` subquery on `leads` (InitPlan-hoisted per 0088, `archived_at IS NULL`); NO user INSERT/UPDATE/DELETE — all writes service-role; the `open → actioned/dismissed` flip is a resolve-once admin-client UPDATE (column restriction enforced in `revival-service`, not SQL); documented in the `COMMENT ON TABLE`. Partial UNIQUE `idx_revival_candidates_one_open (lead_id) WHERE status='open'` = the structural one-open-candidate guard; `idx_revival_candidates_open` serves the review predicate; `idx_revival_candidates_agent_actioned` serves the daily-cap count. **Revival NEVER mutates the leads row** — this is a layer over leads. **⚠️ NOT yet applied to prod.** Regenerate `database.ts` after applying (interim types: `src/lib/types/revival.ts`). |
| `20260613000118_elaya_actions_phase2.sql` | **(Elaya Phase 2 / E3 — agentic writes)** Fills the `elaya_actions` table reserved empty in 0116. Adds partial index `idx_elaya_actions_pending (conversation_id, created_at DESC) WHERE status='proposed'` (the confirmation resolver's per-turn read) + a `COMMENT ON TABLE` documenting the lifecycle. **No columns, no CHECK, no RLS change.** A-11 carve-out: `elaya_actions` is a state-machine table that doubles as an audit trail (has `status`/`resolved_at`/`resolved_by`, unlike append-only `elaya_messages`/`lead_activities`); the `proposed → executed/failed/dismissed` flip is a resolve-once service-role admin-client UPDATE (RLS-bypassing, same posture as `whatsapp_messages` delivery receipts / `task_remarks` suppression) — NOT an A-11 violation, and there must NEVER be a user UPDATE policy. Applied to prod idempotently (`CREATE INDEX IF NOT EXISTS` — the table already existed; 0116 DDL had been applied out-of-band). **⚠️ Ledger drift noted at apply time:** prod was missing ledger rows for 0114/0116/0117 (0116/0117 schema present, 0114 — CAD-02A cadence — NOT applied at all); 0118's row was recorded, the rest flagged for a separate reconciliation. |
| `20260612000117_elaya_reply_log_type.sql` | **(Elaya WhatsApp staff channel)** Widens the `whatsapp_notification_logs.type` CHECK with `'elaya_reply'` — one audit row per outbound Elaya WhatsApp reply attempt (`sendElayaWhatsAppReply` in `whatsapp-api.ts`; same one-log-row-per-attempt `finally` contract as the template senders; last-4 phone digits only). `database.ts` type union hand-extended in the interim. **⚠️ NOT yet applied to prod — apply together with 0116.** |
| `20260614000120_push_subscriptions.sql` | **(Web Push / PWA push channel)** `push_subscriptions` table — per-device Web Push (VAPID) endpoints, the SECOND notification delivery channel behind `createNotification`. Columns: `profile_id` FK, `endpoint` (UNIQUE — one row per device, so one user holds many rows), `p256dh`, `auth`, `user_agent`, `created_at`; `idx_push_subscriptions_profile` serves the dispatch read. **Owner-only RLS** (`profile_id = auth.uid()`): SELECT/INSERT/DELETE for the owner (the browser subscribes/unsubscribes on the session client); NO UPDATE policy (a re-subscribe is an upsert on `endpoint`). The cross-user read + the 404/410 dead-endpoint prune in `dispatchPush` run service-role (admin client), the same posture as `createNotification`'s insert. InitPlan-hoisted `(SELECT auth.uid())` per 0088. **Push is a non-fatal second channel — the in-app notification row stays the source of truth.** Regenerate `database.ts` after applying (interim: hand-extended `push_subscriptions` block + `PushSubscriptionRow` in `database.ts`). **⚠️ NOT yet applied to prod.** |
| `20260615000121_profile_app_icon.sql` | **(PWA home-screen icon picker)** `profiles.app_icon text NOT NULL DEFAULT 'icon-1' CHECK (app_icon IN ('icon-1'..'icon-4'))` — the user's chosen PWA install icon, the `profiles.theme` column pattern exactly. **NO new RLS** — the existing 0001 `profiles_update` policy already permits self-update of non-authorization fields (theme/timezone/app_icon all qualify; the WITH CHECK guard only protects role/domain). NOT added to `log_profile_changes()` (cosmetic, same posture as theme/timezone). The CHECK list is the SQL mirror of `ICON_KEYS` in `lib/constants/app-icons.ts` — a new icon key needs a new migration extending the CHECK (the theme precedent). `database.ts` + `Profile` type hand-extended in the interim (narrowed `app_icon` union). **⚠️ NOT yet applied to prod.** Regenerate `database.ts` after applying. |
| `20260615000123_agent_first_touch_pairs.sql` | **(Performance — first-touch speed scorecard)** `get_agent_first_touch_pairs(p_agent, p_from, p_to)` RPC — returns raw `TABLE(lead_id, created_at, first_call_at)` per cohort lead (assigned + created in period + not archived); `first_call_at = MIN(lead_notes.created_at) WHERE call_outcome IS NOT NULL` (the 0101/0108 "a call" definition), NULL when uncalled. **No bucketing/business-minute math in SQL** — the deck's `< 15m`…`3h+` buckets are measured in BUSINESS minutes per the agent's shift, which is TS-only (`lib/utils/sla.businessMinutesBetween` + `buildAgentShiftOverride`); the service mapper (`getAgentFirstTouchScorecard`, React `cache()`) buckets the raw pairs, so SQL does only the per-lead MIN. **Scope-param RPC (takes `p_agent`) → EXECUTE REVOKED from `PUBLIC, anon, authenticated`, GRANTed `service_role` only** (Q-13 / 0102 posture — admin client only, the gated `assertDrillAccess` action is the trust boundary). `LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public`. **⚠️ NOT yet applied to prod.** |
| `20260615000122_deal_category_and_domain_type.sql` | **(Deal domain→type→category rule)** Makes `deals.deal_type` honest now that it is domain-derived (decision-log 2026-06-15). Adds `deals.deal_category text`; **drops + recreates `deals_deal_type_check`** to admit `'sale'` (house/legacy — the inline 0072 CHECK is named `deals_deal_type_check`, verified via `pg_constraint`, NOT auto-suffixed); adds `deals_deal_category_check` (value whitelist — the SQL mirror of `DEAL_CATEGORIES` in `lib/constants/deal-types.ts`) and `deals_retail_category_check` (the cross-column coupling `retail ⇒ category NOT NULL` / `non-retail ⇒ category NULL`, modelled on `deals_membership_duration_check`). **Order is load-bearing:** the one pre-rule `onboarding+retail` walk-in (no duration, stray `lead_id IS NULL`) is **DELETEd before** the CHECKs so the table is rule-clean; any surviving retail row is backfilled to `'other'` and non-retail categories nulled. `deal_type` is derived server-side in `recordDeal`/`createWalkInDeal` (`DOMAIN_DEAL_CONFIG`) — the CHECKs are the backstop. `Deal` type in `database.ts` hand-extended with `deal_category` (`deals` is not in the generated `Tables` section — service uses `(supabase as any)`). **Applied to prod + verified** (rejects retail-without-category, non-retail-with-category; accepts `sale`). |
| `20260616000124_managers_in_routing_pool.sql` | **(Managers join the round-robin pool)** Brings the routing/shift layer in line with `LEAD_ASSIGNABLE_ROLES` (agents + managers both carry leads). (1) `CREATE OR REPLACE handle_agent_routing_config()` — the `on_agent_profile_created` trigger now auto-creates an `agent_routing_config` row for `role IN ('agent','manager')` (INSERT + role-change-into-pool UPDATE). (2) Backfills config rows for all existing managers (`INSERT … SELECT … ON CONFLICT (agent_id) DO NOTHING`). (3) `CREATE OR REPLACE get_next_round_robin_agent(text)` — both eligibility passes widen `p.role = 'agent'` → `p.role IN ('agent','manager')`; body otherwise identical to 0007; EXECUTE re-revoked from PUBLIC/authenticated (service-role only). Managers sit in the SAME fair queue as agents (oldest-assignment-first), gated on their pool toggle. App-layer mirrors: `ROUTING_POOL_ROLES` in `lib/constants/roles.ts` (alias of `LEAD_ASSIGNABLE_ROLES`), `getAgentRosterByDomain`, the JS `getNextRoundRobinAgent` fallback. The literal `('agent','manager')` list mirrors `ROUTING_POOL_ROLES` — keep in sync. **⚠️ NOT yet applied to prod.** |
| `20260616000125_handle_new_user_job_title.sql` | **(Invite onboarding — job_title)** `CREATE OR REPLACE handle_new_user()` (the `on_auth_user_created` signup trigger) now also copies `job_title` from `auth.users.raw_user_meta_data` into `public.profiles` (`NULLIF(... ,'')`, nullable column). Invites stuff `job_title` into invite metadata but the original 0001 trigger only copied `full_name`/`role`/`domain`, silently dropping it for every invited user (the password-mode `createUser` path set it via a follow-up `updateProfileFields`, so only invites lost it). Body otherwise byte-identical to the 0001 definition. Part of the invite-by-email end-to-end fix (changelog 2026-06-16). **Applied to prod** (`apply_migration`, verified via `pg_get_functiondef`). |
| `20260616000126_usage_tracking.sql` | **(Agent usage / active-time tracking — adoption monitoring)** Two tables + one read RPC. `usage_heartbeats` — **append-only (A-11)** raw active-presence ticks (`user_id`, `domain public.app_domain`, `captured_at`), one row per active user per snapshot-job run; the ONLY writer is `snapshotUsagePresenceTask` (admin client); `usage_heartbeats_user_time_idx (user_id, captured_at)`; pruned > 30 days by the nightly rollup (admin-client maintenance, not a user mutation). `usage_daily` — the rollup the dashboard reads; PK `(day, user_id, domain)` makes the rollup UPSERT idempotent (recompute distinct minute-ticks, **overwrite never increment**); `usage_daily_day_idx (day)`. Both **RLS enabled, NO policies = deny-by-default** — jobs write via the admin client, the dashboard reads only through the RPC. `get_agent_usage(p_today_start timestamptz, p_history_from date)` RPC — `STABLE SECURITY DEFINER SET search_path = public`; returns `{today, history}` jsonb (today = live `COUNT(DISTINCT date_trunc('minute', captured_at))` per user+domain since IST midnight; history = `usage_daily` rows ≥ `p_history_from`); takes **no caller-supplied role/scope** → Q-13 **revoked tier**: `EXECUTE` REVOKEd from `PUBLIC, anon, authenticated`, `GRANT`ed `service_role` only. The founder/admin gate lives in the service layer (`getAgentUsage` via `getCurrentProfile`), never in the RPC. Bigint `active_minutes` `::int` in SQL + `Number()` in the mapper (Q-09). **⚠️ NOT yet applied to prod.** Regenerate `database.ts` after applying (interim types: `src/lib/types/usage.ts`). |
| `20260616000127_lead_activities_actor_nullable.sql` | **(Schema drift fix)** `ALTER TABLE lead_activities ALTER COLUMN actor_id DROP NOT NULL` — restores the column to migration 0003's documented intent (`-- NULL = system/webhook action`). The live DB had drifted to NOT NULL with no DEFAULT (no migration in history added it), which would throw a constraint violation on every system/webhook activity insert in `lead-ingestion.ts` (`lead_created`/`agent_assigned`/`duplicate_submission` with `actor_id: null`) — invisible until a fresh `database.ts` regen generated the accurate non-null `actor_id`. FK + semantics unchanged; re-`COMMENT`s the column. **Applied to prod + verified** (`is_nullable = YES`). |
| `20260617000129_manager_pipeline_full_roster.sql` | **(Manager dashboard — full domain roster in Lead Pipeline)** `CREATE OR REPLACE` of `get_dashboard_summary` (RSC seed) + `get_lead_pipeline_refresh` (widget refresh), patched identically and isolated to the `byAgent` derivation. In the **manager branch only**, `agent_counts` becomes a domain ROSTER CTE (`profiles WHERE domain = p_domain AND role IN ('agent','manager') AND is_active`, mirroring `LEAD_ASSIGNABLE_ROLES`) LEFT JOINed to per-(agent,status) cohort counts — so a teammate (or the manager) with zero leads in the period still renders at `total 0` / `counts {}` (`jsonb_object_agg ... FILTER (WHERE status IS NOT NULL)` + `COALESCE(...,'{}')` guard the all-NULL LEFT-JOIN row). `status_totals` (chip/legend) UNCHANGED. Admin/founder keep the cohort-only `GROUP BY assigned_to` path verbatim (the `ELSE` branch); an admin/founder single-domain tab coerces to `rpcRole='manager'` in the service so it also gets that domain's full roster (intended). Both keep their signatures (no DROP) + re-state the 0102 revoke posture (Q-13, admin-client only). The "You" pin/marker is client-side in `ManagerLeadStatusWidget` (existing `userId` prop) — no identity arg in SQL. **⚠️ NOT yet applied locally (Docker down at author time) — apply + verify.** |
| `20260617000132_recent_lead_activity_rollup.sql` | **(Dashboard "Recent Leads" rollup — SUPERSEDES 0131)** New `get_recent_lead_activity(p_role text, p_domain app_domain, p_user_id uuid, p_scope text DEFAULT 'team')` RPC. Reframes the dashboard "Recent Activity" widget from an EVENT stream into a LEAD rollup: one card per lead, most-recently-worked first (status + latest call outcome + latest note). Queries **`leads`** (not `lead_activities`) — `leads` already denormalises `status`/`last_call_outcome`/`last_activity_at`/`assigned_to`/name/slug/domain, so it's a single descent `WHERE archived_at IS NULL AND last_activity_at IS NOT NULL ORDER BY last_activity_at DESC LIMIT 25` (naturally one row per lead, no aggregation/dedup), LEFT JOIN profiles for `assignee_name`, correlated subquery for the latest `lead_notes.content` (LEFT 160, served by `idx_lead_notes_lead_id`). `p_scope`: `'mine'` → `assigned_to = p_user_id` (any role); `'team'` → role-scoped (agent ALWAYS own — a crafted 'team' can't widen; manager → `p_domain`; admin/founder → `p_domain` when set, else all-org). `LANGUAGE plpgsql STABLE SECURITY DEFINER SET search_path = public`. **Scope-param RPC (Q-13 revoked tier):** EXECUTE REVOKEd from `PUBLIC, anon, authenticated`, GRANTed `service_role` only — the service (admin client) is the trust boundary. **Supersedes 0131** (never applied — Docker down since authored; no deployed function to drop, it's dead). **⚠️ NOT yet applied (Docker down at author time) — apply + regenerate `database.ts`; the interim `as any` `.rpc` cast then drops.** |
| ~~`20260617000131_recent_activity_enrich.sql`~~ | **(REMOVED — superseded by 0132 before it ran)** Was a same-day event-stream enrich of `get_agent_recent_activity`. The widget was reframed to a lead rollup (0132) before 0131 was ever applied (untracked, never committed, Docker down), so the file was **deleted** to avoid creating a now-dead `get_agent_recent_activity` RPC on `migration up`. No code calls `get_agent_recent_activity` anymore. Listed only to explain the gap in the 0131 number. |
| `20260617000133_notification_preferences.sql` | **(Per-user notification control plane)** `notification_preferences` table — per-user In-app/WhatsApp on/off for each notification CATEGORY. PK `(user_id, notification_key)`; `in_app`/`whatsapp` booleans (DEFAULT true); `notification_key` CHECK mirrors the 9 keys in `lib/constants/notification-categories.ts` (`lead_assigned`, `new_lead_founder_alert`, `lead_won`, `deal_created`, `task_assigned`, `task_due`, `task_overdue_manager`, `sla_breach`, `sla_escalation`). **Sparse mute-rows — ABSENCE = ON:** a row exists only as an opt-out; no row → both channels on. The gate (`services/notification-prefs-service.ts`) **fails OPEN** (missing/malformed/thrown → send). **Owner-only RLS** (the `push_subscriptions` 0120 posture): owner SELECT/INSERT/UPDATE/DELETE (`user_id = (SELECT auth.uid())`, InitPlan-hoisted); the cross-user read at notification fan-out runs service-role (admin client), like `dispatchPush`. `update_updated_at` trigger reused. Transactional sends (`lead_initiation`, `elaya_reply`) have NO key — never silenceable. Gate seams: Seam A in `createNotification` (in-app + push, via optional `notificationKey`), Seam B in the 6 broadcast `whatsapp-api.ts` wrappers (per-recipient `filterRecipientsByPref`). `database.ts` interim block + `NotificationPreferenceRow` hand-added. **⚠️ NOT yet applied locally (Docker down at author time) — apply + regenerate `database.ts`; the interim block then drops.** |
| `20260617000134_suggestions_table.sql` | **(Suggestion box / bug-report channel)** `suggestions` table — staff-submitted reports for admin/founder triage. `sender_id` FK, `category` CHECK (`bug`/`idea`/`other`), `message`, `image_paths text[]` (CHECK ≤ 4 — mirrors `MAX_SUGGESTION_IMAGES`; storage PATHS in the private bucket, never URLs), `status` CHECK (`open`/`resolved`), `resolved_by`/`resolved_at`, timestamps; `idx_suggestions_status_created (status, created_at DESC)`; `update_updated_at` trigger reused. **NOT append-only** — the one narrow admin/founder UPDATE policy is the `revival_candidates` carve-out; only `status`/`resolved_by`/`resolved_at` are writable (enforced in `resolveSuggestion`, RLS can't restrict columns). RLS (InitPlan-hoisted per 0088): sender SELECT/INSERT own (`sender_id = (SELECT auth.uid())`); admin/founder SELECT all + UPDATE (`(SELECT get_user_role()) IN ('admin','founder')`); **NO DELETE ever**. `database.ts` not regenerated — interim `SuggestionRow`/`SuggestionWithSender` in `types/suggestions.ts` + `(supabase as any).from('suggestions')` cast in `suggestions-service.ts` (the `revival-service` posture). **⚠️ NOT yet applied — apply + regenerate `database.ts`; the interim types + cast then drop.** |
| `20260617000135_suggestions_storage_bucket.sql` | **(Suggestion screenshots — PRIVATE bucket)** `suggestions` Storage bucket (`public = false` — reports can show sensitive screens, so NO public-read policy, unlike `avatars`/`ad-creatives`). 3 idempotent `storage.objects` policies: `insert` to authenticated pinned to the caller's own uid prefix (`(storage.foldername(name))[1] = (select auth.uid())::text`), `select` own (same check), `select` admin/founder (`(select get_user_role()) in ('admin','founder')` — backs `createSignedUrls` on the admin client). No UPDATE/DELETE. Object path convention `${sender_id}/${draftId}/${i}-${file}`; the action re-checks the prefix (defence in depth). Image URLs are minted on read via short-TTL signed URLs — never stored. |
| `20260617000136_notification_type_suggestion_resolved.sql` | **(Suggestion resolve-notify)** Widens `notifications.type` CHECK with `'suggestion_resolved'` (DROP + re-ADD `notifications_type_check`, re-listing all 10 existing 0113 values + the new one). Fired by `resolveSuggestionAction` → `createNotification` (transactional, no `notificationKey` — never silenceable, the `lead_initiation`/`elaya_reply` posture). `database.ts` `NotificationType` union hand-extended in the interim. **⚠️ NOT yet applied — apply + regenerate `database.ts`.** |
| `20260617000137_lead_phone_dedup_uniqueness.sql` | **(Lead-creation audit 2026-06-17)** `lead_phone_key(text)` IMMUTABLE canonical-phone key (digits-only collapse, mirrors `canonicalizePhone()` in `lib/utils/phone.ts` + the existing `generate_lead_slug` regex) + partial UNIQUE index `idx_leads_phone_key_active` on `lead_phone_key(phone)` for ACTIVE leads only (`archived_at IS NULL`, non-empty phone, status `new/touched/in_discussion/nurturing` — MUST match `get_active_lead_by_phone`). THE structural backstop for the dedup TOCTOU race (no UNIQUE on `leads.phone` existed): a concurrent insert that races past the SELECT-based dedup now gets `23505`, caught in `ingestLead`/`createManualLead`/`createLeadFromWhatsApp` and resolved to the existing active lead. Terminal/archived leads excluded so the `previous_lead_id` returning-prospect chain is never blocked. **Dirty-data safe:** a DO block detects pre-existing active-phone collisions and falls back to a NON-unique index with `RAISE WARNING` (lists keys) rather than hard-failing; resolve + re-create UNIQUE after. **⚠️ NOT yet applied.** |
| `20260616000128_revival_silent_leads_rpc.sql` | **(Lead Revival — sweep scaling, audit #8/#14)** `get_silent_leads_for_revival(p_status text, p_threshold timestamptz, p_limit integer)` RPC — pushes the daily sweep's judge-once anti-join into Postgres. `findSilentLeadsForStatus` previously SELECTed EVERY `revival_candidates.lead_id` into a Node `Set` and inflated the leads `LIMIT` by that count — both the transfer and the LIMIT grew unbounded with the ledger. The RPC returns up to `p_limit` silent leads in the trigger status with NO candidate of ANY status (`NOT EXISTS`, served by `idx_revival_candidates_lead`), same silence-clock semantics (`status_changed_at < threshold AND (last_activity_at NULL or < threshold)`), `ORDER BY status_changed_at ASC`. `LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public`. **Scope-param RPC (Q-13 revoked tier):** EXECUTE REVOKEd from `PUBLIC, anon, authenticated`, GRANTed `service_role` only — the sweep (admin client) is the trust boundary. **⚠️ NOT yet applied locally (Docker was down at author time) — apply + regenerate `database.ts`, then the interim `as any` cast on the `.rpc` call in `revival-service.ts` can drop.** |
| `20260620000139_ad_account_recharges.sql` | **(Budget — per-account recharge ledger)** `ad_account_recharges` table — finance record of money sent to each Meta ad account, SEPARATE from `ad_spend_daily` spend. Mirrors the `ad_spend_daily` (0104) table/RLS/trigger pattern. `ad_account text` CHECK in (`april`,`gmr`,`dubai`) — the SQL mirror of `AD_ACCOUNT_KEY_VALUES` in `lib/constants/ad-accounts.ts` (extend with "Indulge New Gen" later = this CHECK + one `AD_ACCOUNTS` line); `amount numeric(12,2)` CHECK `> 0`; `currency` default INR; `recharged_at date`; `done_by uuid → profiles`; `platform` default meta; `method`/`note` free-text labels. **PII backstop:** `ad_account_recharges_no_card_pan` CHECK rejects any `method`/`note` containing a 13–19 digit run (card PAN, space/hyphen-tolerant) — defence in depth behind the Zod refine + action re-sanitize. Indexes `(recharged_at DESC)` + `(ad_account)`; reuses `update_updated_at()`. RLS: manager+ SELECT, admin/founder INSERT/UPDATE/DELETE — **hard DELETE permitted (mirrors `ad_spend_daily`), NOT append-only** (a recharge is an editable finance figure, a fat-fingered amount must be correctable). Account on the SPEND side is DERIVED from the campaign key (`resolveAccountFromCampaign`, index-2 segment), never stored — `ad_spend_daily` untouched. **⚠️ NOT yet applied (Docker down at author time) — apply + regenerate `database.ts`; the interim `(supabase as any).from('ad_account_recharges')` casts in `ad-spend-service.ts` / `actions/recharge.ts` then drop.** |
| `20260623000140_cold_lead_cutoff_dry.sql` | **(Going-cold DRY — single cutoff source)** Kills the going-cold predicate duplication. (1) New `public.cold_lead_cutoff()` — `STABLE` SQL fn returning `now() - interval '5 days'`; THE single SQL source of the cold cutoff, its interval pinned to `COLD_LEAD_THRESHOLD_DAYS` in `lib/constants/leads.ts` (the only two places the number lives). (2) `CREATE OR REPLACE get_dashboard_summary` — recreated from the **live deployed body** (which had drifted ahead of every on-disk migration: 0138-shape `tgm.lead_id IS NOT NULL` task CTE + roster CTE + `pending_calls_count`/`new_leads_count`); the ONLY change vs live is the cold predicate `v_now - interval '5 days'` → `cold_lead_cutoff()`. Reconciles file ⇆ DB. GRANT to `authenticated` re-stated (CREATE OR REPLACE drops it). TS side switched the three inline `new Date(Date.now() - …)` copies (`getLeadsByRole`, `getLeadsForExport`, `getGoingColdLeads`) to the new `goingColdCutoff()` helper. **Rolling window preserved (now − 5×24h), zero behaviour change** — verified old-literal vs new-fn cold count identical (2283) on prod before + after. **Applied to prod via MCP + verified** (predicate uses fn, no literal remains, GRANT intact, count unchanged). `cold_lead_cutoff` is a pure no-input clock helper → keeps its default `authenticated` GRANT (not a scope-param RPC; Q-13 N/A). |
| `20260624000143_dashboard_cold_leads_honor_domain.sql` | **(Going-cold count honours the domain selector)** `CREATE OR REPLACE get_dashboard_summary` from the live 0140 body, changing ONLY the cold-leads predicate to honour `p_initial_domain` (the global serene-domain pick) the same way the `lead_status` + `campaigns` CTEs already do: manager → `p_domain`, admin/founder → `p_initial_domain` (the pick) or all-org. Before this, the cold count was the ONE widget hardcoded org-wide for admin/founder (`p_role IN ('admin','founder') THEN true`) — it never changed when the domain selector changed. Everything else byte-identical to 0140; cutoff stays `cold_lead_cutoff()`, date filter still not applied (live state). GRANT re-stated (CREATE OR REPLACE drops it; 0102 revoke posture preserved — self/role-scoped, keeps `authenticated`). **Applied to prod via MCP + verified by calling the RPC per domain: all-domains 2286, onboarding 1154, shop 730, house 329, legacy 73 (sum = 2286).** (First MCP apply hit a transient Cloudflare 502 and did NOT land — verified by calling the RPC, not just reading `pg_get_functiondef`; re-applied successfully.) |
| `20260623000141_whatsapp_media_bucket.sql` | **(WhatsApp inbound-media durability)** New **private** `whatsapp-media` Storage bucket (`public=false`) — holds inbound (later outbound) WhatsApp media downloaded off Gupshup's time-limited CDN so it persists. Mirrors the `suggestions` (0135) private-bucket pattern, NOT the public `avatars`/`ad-creatives`. One defence-in-depth admin/founder `SELECT` policy on `storage.objects` (InitPlan-hoisted `get_user_role()`); **no authenticated INSERT/UPDATE/DELETE** — writes + signed-url reads run through the **admin (service-role) client** (the inbound webhook has no session; the page/action role layer is the trust boundary, same posture as `whatsapp_messages` RLS). Object path `{leadId}/{messageId}.{ext}`; `whatsapp_messages.media_url` stores the PATH (never a url), `signMediaPath` mints a 1-hour signed url on read. Service: `src/lib/services/whatsapp-media.ts`. **Applied to prod via MCP + verified** (bucket exists, `public=false`). |
| `20260624000141_backfill_campaign_account_segment.sql` | **(Budget — account attribution backfill)** Data migration (DML, not DDL). Rewrites `leads.utm_campaign` on ALL leads (active + archived) to the account-bearing naming convention `TG_<Domain>_<Account>_<Type>_<Date>` so the budget page's `resolveAccountFromCampaign` (index-2 / slot-3 segment → `april`/`gmr`/`dubai`) can attribute spend, and so old leads merge with the in-place-renamed Meta campaigns' new leads into ONE campaign. Mapping = the reviewed `final.csv` (root, user is source of truth), validated row-by-row against live data. 17 distinct keys collapse via intentional merges (Goa Resort typo'd name + canonical = 244 leads; 6 Bogey golf rows → 1; 4 Meta ad-set-id leaks the user verified against Meta → their named campaigns; `/` → Organic). Non-Meta traffic (Organic, Google Search `TG-(Leads\|Search)-*`, `Google_Ads`, `meta_luxury_new`, `TG-Remarketing-Dec25`) deliberately left WITHOUT an account → resolves "Unattributed" (the honest result). **Result: 5 → 3,217 leads attributable; april 2,877 / gmr 225 / dubai 115 / unattributed 411; 0 leads lost, 81 → 64 distinct keys.** Isolated write: `utm_campaign` has no FK / generated-column dep (`search_text` excludes it), only `updated_at` UPDATE trigger, `trg_lead_slug` is INSERT-only. NOT idempotent (each WHERE keys off the OLD name, gone after first run). **Applied to prod via MCP `apply_migration` + verified** (total leads unchanged 5181, zero old names remain). |
| `20260624000142_task_agent_reminder_log_types.sql` | **(Task reminders — agent + non-lead-manager WhatsApp pings)** Extends `whatsapp_notification_logs.type` CHECK (DROP + re-ADD, re-listing the six 0113 values + `elaya_reply` from 0117) with three new LOG types: `task_due_soon` (the -30m "due soon" WhatsApp to the assigned agent, `sendTaskDueSoonTask`) + `task_overdue_agent` (the at-due overdue WhatsApp to the assigned agent, in `sendTaskReminderTask`) + `task_overdue_manager_generic` (the due+threshold overdue escalation for a NON-lead task to the assignee's manager — `reports_to` → domain fallback — via `checkTaskOverdueTask`'s non-lead branch). All lead-agnostic + task-shaped; the agent pings ride the EXISTING `task_due` key, the generic manager escalation rides `task_overdue_manager` — **no `notification_preferences` CHANGE** (log types, not gate categories). All three templates wired live (`eia_task_due_soon` / `eia_task_overdue_agent` / `eia_task_overdue_manager_generic`). **Applied to prod via MCP + verified 2026-06-24** (constraint carries all 10 values). |
| `20260624000144_oversight_task_events.sql` | **(Oversight — `task_events` stream + 3 read RPCs)** Backs the `/oversight` 3-tier manager+ drill. **`task_events`** append-only table: `task_event_type` enum (`created`/`status_changed`/`reassigned`/`remark_added`/`overdue`); `task_id`→tasks CASCADE, `domain app_domain NOT NULL`, `actor_id`/`subject_id`→profiles, `task_title` snapshot, `meta jsonb`, `created_at`; indexes `(domain, created_at DESC)` + `(subject_id, created_at DESC)`. **RLS: manager+ SELECT only; NO INSERT/UPDATE/DELETE policy — ever (A-11).** Realtime ENABLED. Written ONLY by the task-mutation cores (`task-mutations.ts`) + `addTaskRemarkAction` + `checkTaskOverdueTask`, via the admin client (`emitTaskEvent`/`resolveTaskDomain` in `services/task-events.ts`). Three **SECURITY DEFINER scope-param** RPCs — EXECUTE REVOKEd from `PUBLIC/anon/authenticated`, GRANTed to `service_role` (Q-13, 0102 pattern), admin-client only: **`get_team_task_overview(p_role, p_domain)`** (Tier 1 — one row per rostered `app_domain`: agent_count + open/overdue/completed/in_review tallies), **`get_team_agent_breakdown(p_role, p_caller_domain, p_domain)`** (Tier 2 — per-agent tallies in a team), **`get_agent_tasks_oversight(p_agent, p_role, p_caller_domain)`** (Tier 3 — an agent's task rows + lead identity via `task_gia_meta` LEFT JOIN, meta-presence; the service mapper derives the counts). All three derive task→domain via `COALESCE(task_groups.domain, assignee.profiles.domain)` (no `tasks.domain` column — group → group domain, else assignee's) and **force-clamp a manager to their own domain in SQL** (the manager `tasks` SELECT RLS is role-only — RLS cannot isolate teams; the action + page + RPC are the three layers). **Applied to prod via MCP `apply_migration` + verified 2026-06-24** (REVOKE confirmed `none` to authenticated, `task_events` SELECT-only, manager clamp returns own-team/zero-rows, a `status_changed` event readable via both rail filters with zero `task_remarks` rows). `database.ts` types pending regen — interim hand-declared `lib/types/oversight.ts` + `as any` `.rpc`/`.from` casts (the `revival`/`usage`/`elaya` pattern). |
| `20260617000138_collapse_gia_category_module_enum.sql` | **(Task model — collapse `gia_followup`, model module-links by meta table)** Collapses `tasks.task_category` from three values to **two structural** ones — `tasks_category_check` re-constrained to `('personal','group_subtask')`. A lead follow-up is now a `personal` task that **HAS a `task_gia_meta` row** (the meta table IS the link; the category no longer encodes it). `tasks.module` converted free `text` → native enum **`task_module ('gia','sia','core')`** (`'gia'` lead follow-up, `'sia'` future tickets, `'core'` everything else); `idx_tasks_module` dropped/recreated around the retype. **THE SINGLE-WRITER INVARIANT (load-bearing):** a `task_gia_meta` row exists IFF the task is a lead follow-up AND `module='gia'` IFF a meta row exists — because `create_lead_gia_task` (+ the `update_lead_status` nurturing branch) is the SOLE writer of both, always together; every other insert writes `module='core'` + no meta row. So `EXISTS(task_gia_meta)` / `tgm.lead_id IS NOT NULL` permanently substitutes for the retired `task_category='gia_followup'` check — the rewritten reads BORROW correctness from the write path (commented inline at each site). **Repointed (no signature change):** `create_lead_gia_task` + `update_lead_status` nurturing → insert `personal`+`module='gia'`+meta row; `get_gia_tasks` → drops the category predicate (its `INNER JOIN task_gia_meta` already scopes); `get_dashboard_summary` (live 0129) → §1 agent-tasks CASEs + LEFT JOIN and the agent `pending_calls_count` re-keyed to `tgm.lead_id IS NOT NULL` / `EXISTS(task_gia_meta)` (diff-verified byte-identical to 0129 except those 4 spots; re-states the 0102 revoke posture). **Index hygiene (§6):** `idx_tasks_tags_gin` / `idx_tasks_tags_active` guarded `AND module <> 'gia'` (a per-row column test — partial-index predicates can't use a subquery; standalone-personal-only, no lead-task pollution); `get_personal_tasks` deliberately WIDENED (lead tasks SHOULD appear in the My Tasks calendar). **§0 one-time data cleanup:** 60 pre-`0054` lead-less `gia_followup` orphans (no meta row, irrecoverable link) resolved — 35 terminal DELETEd, 25 active → `cancelled` (audit-preserving; DELETE-before-cancel order is load-bearing); §1 backstop then hard-fails on any remaining ACTIVE orphan. **NOT touched:** `sla_policies.trigger_value='gia_followup'` (a DIFFERENT column — the SLA "task due" catalog, 0111). **Applied to prod via MCP + verified** (`module='gia'` count = `task_gia_meta` count = 149, zero invariant violations, zero `gia_followup` remaining). `database.ts` regenerated (`TaskCategory` dropped `gia_followup`; `module` typed as the enum). |

| `20260625000145_personal_tasks_lead_identity.sql` | **(My Tasks — show the linked lead on lead follow-up tasks, Q2)** Widens `get_personal_tasks` from `RETURNS SETOF tasks` to `RETURNS TABLE(tasks.* + lead_id/lead_first_name/lead_last_name/lead_slug)` via `LEFT JOIN task_gia_meta → leads`. A lead follow-up (a `personal` task with a `task_gia_meta` row since 0138) has a type-derived title ("Call"); the join surfaces WHICH lead so My Tasks renders "Call · Sonu Singh" + a dossier link. Non-lead personal tasks LEFT-JOIN to NULL (unchanged); the 0138 single-writer invariant guarantees ≤1 meta row per task (no fan-out). WHERE/cursor/ORDER BY/params byte-identical to the live 0026 body — only the SELECT list + return type change; columns enumerated in `tasks` ordinal order (the `get_gia_tasks`/`get_active_lead_by_phone` v2 no-`SELECT *`-after-join pattern). DROP required (return-type change). Self-scoped → keeps `GRANT EXECUTE TO authenticated` (no Q-13 change). **Applied to prod via MCP + verified** (a real `module='gia'` "Call" task returns its lead name; `authenticated` EXECUTE intact). Service: `PersonalTaskRow` type + `PersonalTasksResult.tasks: PersonalTaskRow[]`; Redis `personalPage1` key bumped `:v2` to retire pre-0145 narrow envelopes. |
| `20260625000146_agent_performance_trend.sql` | **(Agent `/performance` lean self-scorecard)** `get_agent_performance_trend(p_date_from, p_date_to)` RPC — **SELF-SCOPED** (`auth.uid()`, no identity/scope params → keeps `GRANT EXECUTE TO authenticated`, the `get_agent_today_pulse` 0108 posture, **not** the Q-13 revoked tier). Returns one bucket per IST calendar day in the period (`generate_series`, oldest-first, zero-filled): `leads_won` (won by `status_changed_at`, the 0101 def), `calls` (`call_outcome IS NOT NULL`, the 0101/0108 def), `notes` (all agent notes, superset of calls). Day bucketing IST via `AT TIME ZONE 'Asia/Kolkata'` (the 0108/0122 trend convention); rate metrics deliberately absent (a daily rate off 0–2 closes is noise). Backs the period `AgentActivityTrendChart` + the one honest Leads-Won KPI sparkline (replaced the fabricated `makeSpark`). Additive — no existing function touched. `LANGUAGE plpgsql STABLE SECURITY DEFINER SET search_path = public`. **⚠️ NOT yet applied to prod** — interim `(supabase as any).rpc` cast in `performance-service.ts` until `database.ts` regen. |
| `20260626000151_customer_welcome_blast.sql` | **(Customer welcome-blast Blocks 3–4 — the outward-facing customer channel)** Two changes. (1) `leads.welcomed_at timestamptz` — the one-blast-per-lead idempotency stamp (NULL = never welcomed); set once by the customer-Elaya send path under an `UPDATE … WHERE welcomed_at IS NULL RETURNING` guard (the exactly-once `markTaskOverdueOnce`/`overdue_at` pattern) so the welcome template fires exactly once. A layer over leads — NOT a lifecycle column. (2) widened `whatsapp_notification_logs.type` CHECK (DROP + re-ADD, re-listing the 10 existing values from 0117/0142) with `customer_welcome` (the approved Gupshup welcome TEMPLATE — the first cold-number touch) + `customer_reply` (a free-form session reply Elaya sends a customer in the 24h window — the blast + conversation). Mirrors the 0117 `elaya_reply` addition. **Applied to prod via MCP + verified** (welcomed_at present, CHECK carries both new types). `leads.welcomed_at` hand-added to `database.ts` (Row/Insert/Update) in the interim. |
| `20260626000150_elaya_training_assets.sql` | **(Customer welcome-blast Block 2 — the training library)** New `elaya_training_assets` table — Elaya's curated customer-facing material (kind/title/description/url/storage_path/`tags text[]`/`domain app_domain` nullable/`send_order`/active + created_at/updated_at). EDITABLE config/content (NOT A-11 — carries `update_updated_at()` trigger + UPDATE/DELETE policies, the `ad_creatives` 0012 posture). `kind` CHECK = the SQL mirror of the 10 `TRAINING_ASSET_KINDS` in `lib/constants/elaya-training.ts` (brochure/work_example/testimonial/review/podcast/image/video/doc/fact/url). RLS: **all-authenticated SELECT** (every staff Elaya turn resolves assets); **manager/admin/founder** INSERT/UPDATE/DELETE via `(SELECT get_user_role())` (managers curate their domain's library — the locked decision; the role-gated action is the second A-09 layer). Indexes (domain, active, send_order). New **PUBLIC** `elaya-training` storage bucket (so Gupshup can fetch a sent media url with no signing — the avatars/ad-creatives posture, NOT the private whatsapp-media one) + 4 `storage.objects` policies (public read; manager+ insert/update/delete). The company-facts brief is the per-domain `kind='fact'` row. **Applied to prod via MCP + verified** (12 cols, 4 table + 4 storage policies, RLS on, bucket public, CHECK = the 10 ids). `database.ts` types pending regen — interim `lib/types/elaya-training.ts` + `(… as any).from('elaya_training_assets')` casts (the revival/suggestions pattern). |
| `20260626000153_task_assigned_log_type.sql` | **(Task-assigned WhatsApp notification)** Widens the `whatsapp_notification_logs.type` CHECK (DROP + re-ADD, re-listing the 12 existing values from 0151) with `task_assigned` — the log type for the new "a task was assigned to you by someone" WhatsApp template (`sendTaskAssignedNotification`, fires to the ASSIGNEE on a personal-task-to-another or a group subtask). Gated by the EXISTING `task_assigned` control-plane key (0133) — a new LOG type, NOT a new gate category. Mirrors the 0142/0117/0151 widenings. **Applied to prod via MCP + verified.** Template id is env-driven (`GUPSHUP_TASK_ASSIGNED_TEMPLATE_ID`) — no-ops until set. |
| `20260626000152_elaya_notes.sql` | **(Elaya Notes section — Jarvis Feature 3 / Block 4)** New `elaya_notes` table — per-user free-form notes Elaya READS as CONTEXT (never permission). `user_id` FK (`ON DELETE CASCADE`), `title`, `body` (DEFAULT `''`), created_at/updated_at. EDITABLE personal content (NOT A-11 — `update_updated_at()` trigger + UPDATE/DELETE policies, the `elaya_training_assets`/`suggestions` posture). **Owner-only RLS** (the `push_subscriptions` 0120 / `notification_preferences` 0133 posture): SELECT/INSERT/UPDATE/DELETE all gated `user_id = (SELECT auth.uid())` (InitPlan-hoisted per 0088). The Elaya TURN read (`getNotesForElaya`) runs **service-role scoped by `principal.userId` in code** — the channel-parity rule (`auth.uid()` is NULL on the sessionless WhatsApp webhook, so RLS would blank). `idx_elaya_notes_user (user_id, updated_at DESC)` serves both the page list + the turn read. Backs `retrieveMemoryContext` (memory.ts — now LIVE, was `notes: []`). **Applied to prod via MCP + verified** (6 cols, RLS on, 4 policies, 1 trigger, PK + user index). `database.ts` types pending regen — interim `lib/types/elaya-notes.ts` + `(… as any).from('elaya_notes')` casts. |

## Multi-write RPC pattern (perf-02)

When a server action performs multiple sequential DB writes that must be atomic, move all writes into a `SECURITY DEFINER` RPC. Access control (Zod validation, auth check, role/domain verification) stays in the action layer. Application-layer side-effects (Trigger.dev, notifications, SLA scheduling) also stay in the action layer — they cannot go inside a Postgres function.

**Pattern established by:** `add_lead_call_note` (migration 0030) and `update_lead_status` (migration 0031).

**Rules:**

- RPC must be `SECURITY DEFINER SET search_path = public` (A-10).
- Never add access-control logic inside the RPC — it runs as the function owner, not the calling user.
- Return a `jsonb` object with everything the action needs for fire-and-forget side-effects.
- Use `RAISE EXCEPTION` for not-found cases; the action maps these to user-facing errors.
- Cast `bigint` COUNT fields to `Number()` in the service/action layer before use (Q-09).

## whatsapp_messages — append-only contract with delivery-receipt exception

`whatsapp_messages` is append-only. There is no DELETE policy and no UPDATE policy for app users.

The one narrow exception: delivery receipt status updates (`status`, `status_at`) are written by the webhook handler using the **service-role client** (`src/lib/supabase/admin.ts`), which bypasses RLS. This is a system write, not a user mutation — it satisfies A-11.

PostgreSQL RLS cannot restrict which columns may be updated within an eligible row. Column restriction (only `status` and `status_at`) is enforced exclusively at the application layer in the webhook handler. Future engineers: do not add an UPDATE policy for agent/manager roles.

**wa_message_id uniqueness:** Enforced via a **partial unique index** (`WHERE wa_message_id IS NOT NULL`), not a column `UNIQUE` constraint. This allows multiple rows with `wa_message_id = NULL` for optimistic pre-confirm inserts. A full unique constraint would reject these rows.

**Realtime:** Both `whatsapp_conversations` and `whatsapp_messages` are on `supabase_realtime`. The WhatsApp page subscribes to both channels.

**can_access_wa_conversation():** SECURITY DEFINER helper defined in migration 0032. Used by both `whatsapp_conversations` and `whatsapp_messages` RLS policies. If `leads` RLS ever changes, review this function too — they are coupled.

---

## task_remarks — append-only contract with suppression exception

`task_remarks` replaces `task_messages` (dropped in migration 0022, pre-production, no data loss).

`task_remarks` has SELECT, INSERT, and one narrow UPDATE policy (`task_remarks_suppression_update`).
There is no DELETE policy and no other UPDATE policy.

The UPDATE policy permits admin/founder to update `task_remarks` rows. PostgreSQL RLS does NOT
restrict which columns may be updated — only which rows are eligible. Column restriction (only
`is_suppressed`, `suppressed_by`, `suppressed_at` may change) is enforced exclusively at the
application layer in `suppressTaskMessageAction` (`src/lib/actions/tasks.ts`). Future engineers:
do not assume SQL prevents changes to `content`, `author_id`, `task_id`, or `status_change`.

**status_change column:** nullable text. Set when a remark accompanied a status transition.
CHECK values mirror `tasks.status` CHECK exactly — they are coupled and must stay in sync.
If `tasks.status` ever gains a new value, a new migration must extend `task_remarks.status_change` too.

**Suppressed remarks** are never deleted. `is_suppressed = true` causes the UI to render
"This message was removed." — original content is never shown for any role.

**Index:** `idx_task_remarks_task_id` on `(task_id, created_at ASC)` — ASC because timeline reads oldest-first.

## task_audit_log — trigger contract

`log_task_changes()` fires AFTER UPDATE on `tasks` FOR EACH ROW. It logs changes to exactly six
fields: `title`, `description`, `status`, `priority`, `due_at`, `assigned_to`. All other columns
are intentionally excluded.

`changed_by` defaults to `auth.uid()`. When `auth.uid()` is NULL (service-role context), falls
back to `NEW.assigned_to`. Known limitation: reassignment via service role logs the new assignee
as the changer. Accepted trade-off — do not add a `changed_by` column to `tasks` to compensate.

`task_audit_log` is append-only — no UPDATE or DELETE policies ever.

## tasks status vocabulary (post-0017)

Old values `pending` and `done` no longer exist in the DB. They were migrated to `to_do` and `completed` respectively. Any code referencing the old values will fail at the DB constraint level.

New full set: `to_do | in_progress | in_review | completed | error | cancelled`

## tasks.task_type vocabulary (post-0057)

Allowed values: `call | whatsapp_message | other`.

Legacy values `email` and `general_follow_up` were backfilled to `other` in migration 0057.
UI labels live in `src/lib/constants/task-types.ts`. Nurturing auto-task in `update_lead_status` RPC uses `other`.

Never reintroduce `general_follow_up` or `email` in application writes.
